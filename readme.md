1. git init
2. ssh code
3. git add .
4. git commit -m"."
5. git push origin main

\*\* git pull (to check everything up to date)

\*\* remove any file from repo:
git rm .file_name
git commit -m "."
git push
